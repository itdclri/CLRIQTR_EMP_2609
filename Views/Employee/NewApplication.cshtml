@model CLRIQTR_EMP.Models.NewApplicationModel

@{
    ViewBag.Title = "New Application - Step 1";
    Layout = "~/Views/Shared/_Layout.cshtml";

    var entitledTypeOccupied = ViewBag.EntitledTypeOccupied != null
        && (bool)ViewBag.EntitledTypeOccupied;

    var showScientistOption = ViewBag.ShowScientistOption != null
        && (bool)ViewBag.ShowScientistOption;

    var isSaEligible = ViewBag.IsSaEligible != null
        && (bool)ViewBag.IsSaEligible;

    var isSaOccupied = ViewBag.IsSaOccupied != null
        && (bool)ViewBag.IsSaOccupied;

    var alreadyResident = ViewBag.AlreadyResident != null
        && (bool)ViewBag.AlreadyResident;

    var currentResidence = ViewBag.CurrentResidence as string ?? "";


    var quartersTypeLabel = "Applying for Quarters Type";
    if (!string.IsNullOrEmpty(Model.QuarterType))
    {
        quartersTypeLabel += " - " + Model.QuarterType;
    }

    /*var payLevelRaw = Session["PayLevel"]?.ToString() ?? "";
    int payLevel = 0;

    // Extract digits only, to handle cases like "13 a"
    var digitsOnly = new string(payLevelRaw.Where(char.IsDigit).ToArray());
    int.TryParse(digitsOnly, out payLevel);

    var designation = Session["Designation"]?.ToString() ?? "";

    bool showScientistOption = false;

    if (payLevel > 7)
    {
        showScientistOption = true;
    }
    else if (payLevel == 7 && designation.Equals("Technical Officer", StringComparison.OrdinalIgnoreCase))
    {
        showScientistOption = true;
    }*/
}

<style>
    .navbar-nav .nav-link.active {
        font-weight: bold;
        color: #0d6efd; /* Bootstrap primary color */
    }
</style>

<nav class="navbar navbar-expand navbar-light bg-light mb-4">
    <div class="container">
        <ul class="navbar-nav me-auto">
            <li class="nav-item">
                <a class="nav-link active" href="@Url.Action("Index", "Employee")">
                    <i class="bi bi-file-earmark-plus"></i> New Application
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" aria-current="page" href="@Url.Action("ViewDrafts", "Employee")">
                    <i class="bi bi-folder-fill"></i> My Applications
                </a>
            </li>
        </ul>

        <div class="d-flex align-items-center ms-auto">
            <div class="navbar-text me-3">
                <i class="bi bi-person-badge"></i> Employee Number : <b>@Session["EmpNo"]</b>
            </div>
        </div>

        <ul class="navbar-nav mb-0">
            <li class="nav-item">
                <a class="nav-link text-danger" style="font-size: 1.1rem;" href="@Url.Action("Logout", "Login")">
                    <i class="bi bi-box-arrow-right"></i> Logout
                </a>
            </li>
        </ul>
    </div>
</nav>

<h2 class="mt-4 text-center">New Application</h2>

<div class="card mt-4 shadow-sm p-4">
    @using (Html.BeginForm("NewApplication", "Employee", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        if (alreadyResident && !string.IsNullOrEmpty(currentResidence))
        {
            <div class="mb-4 row align-items-center">
                <div class="col-md-4 pe-4">
                    <label class="form-label fw-bold">Current CLRI Accommodation</label>
                </div>
                <div class="col-md-8">
                    <span class="form-control-plaintext">@currentResidence</span>
                </div>
            </div>
        }


        <!-- Quarters Toggle -->
        if (!entitledTypeOccupied)
        {
            <div class="mb-4 row align-items-center">
                <div class="col-md-4 pe-4">
                    <label class="form-label fw-bold">@quartersTypeLabel</label>
                </div>
                <div class="col-md-8 d-flex align-items-center">
                    <div class="form-check form-switch">
                        @Html.CheckBoxFor(m => m.ApplyingForQuarters, new { @class = "form-check-input", id = "quartersToggle" })
                        <label class="form-check-label ms-2" for="quartersToggle">Yes / No</label>
                    </div>
                    @Html.ValidationMessageFor(m => m.ApplyingForQuarters, "", new { @class = "text-danger ms-3" })
                </div>
            </div>
        }
        else
        {
            <div class="mb-4 row align-items-center">
                <div class="col-md-4 pe-4">
                    <label class="form-label fw-bold">@quartersTypeLabel</label>
                </div>
                <div class="col-md-8">
                    <span class="text-muted">
                        Already in occupation of Type @Model.QuarterType quarters.
                        
                    </span>
                </div>
            </div>
        }

        if (showScientistOption)
        {
            <div class="mb-4 row align-items-center">
                <div class="col-md-4 pe-4">
                    <label class="form-label fw-bold">Applying for Scientist Quarters</label>
                </div>
                <div class="col-md-8 d-flex align-items-center">
                    <div class="form-check form-switch">
                        @Html.CheckBoxFor(m => m.ApplyingForScientistQuarters,
                            new { @class = "form-check-input", id = "scientistToggle" })
                        <label class="form-check-label ms-2" for="scientistToggle">Yes / No</label>
                    </div>
                    @Html.ValidationMessageFor(m => m.ApplyingForScientistQuarters, "",
                        new { @class = "text-danger ms-3" })
                </div>
            </div>
        }
        else if (isSaOccupied)
        {
            <div class="mb-4 row align-items-center">
                <div class="col-md-4 pe-4">
                    <label class="form-label fw-bold">Applying for Scientist Quarters</label>
                </div>
                <div class="col-md-8 d-flex align-items-center">
                    <span class="text-muted">Already in occupation of Scientist Apartments.</span>
                </div>
            </div>
        }
        @* If not eligible AND not occupied → completely suppress the row *@

        <!-- Submit -->
        <div class="text-center">
            <button type="submit" class="btn btn-primary px-5">Next</button>
        </div>
    }
</div>

@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const form = document.querySelector("form");
            const quartersToggle = document.getElementById("quartersToggle");
            const scientistToggle = document.getElementById("scientistToggle");

            let errorMessageDiv = document.createElement("div");
            errorMessageDiv.className = "text-danger text-center mt-2";
            errorMessageDiv.style.display = "none";
            form.appendChild(errorMessageDiv);

            const q = document.getElementById("quartersToggle");
            if (q) q.checked = false;

            const sa = document.getElementById("scientistToggle");
            if (sa) sa.checked = false;

            form.addEventListener("submit", function (e) {
                const quartersExists   = !!quartersToggle;
                const scientistExists  = !!scientistToggle;

                const applyingForQuarters          = quartersExists  ? quartersToggle.checked  : false;
                const applyingForScientistQuarters = scientistExists ? scientistToggle.checked : false;

                let errorText = "";

                if (quartersExists && scientistExists) {
                    // Both options are visible → at least one must be Yes
                    if (!applyingForQuarters && !applyingForScientistQuarters) {
                        errorText = "At least one option (Quarters / Scientist Quarters) must be 'Yes'.";
                    }
                } else if (quartersExists && !scientistExists) {
                    // Only normal Quarters toggle visible → it must be Yes
                    if (!applyingForQuarters) {
                        errorText = "Please select 'Yes' to apply for Quarters Type - @Model.QuarterType.";
                    }
                } else if (!quartersExists && scientistExists) {
                    // Only Scientist toggle visible → it must be Yes
                    if (!applyingForScientistQuarters) {
                        errorText = "Please select 'Yes' to apply for Scientist Quarters.";
                    }
                }

                if (errorText) {
                    e.preventDefault();          // stop going to next page
                    errorMessageDiv.innerText = errorText;
                    errorMessageDiv.style.display = "block";
                } else {
                    errorMessageDiv.innerText = "";
                    errorMessageDiv.style.display = "none";
                }
            });
        });
    </script>
}

